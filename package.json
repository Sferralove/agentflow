{
  "name": "agent-flow-monorepo",
  "private": true,
  "workspaces": [
    "server",
    "plugin"
  ],
  "scripts": {
    "build": "npm run build -w server",
    "build:plugin": "npm run build -w plugin",
    "test": "npm run test -w server",
    "dev": "npm run dev -w server"
  }
}
